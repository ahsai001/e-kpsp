<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.ui.fkm.e_kpsp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.PasienListActivity"></activity>
        <activity android:name=".pages.ReportActivity" />
        <activity android:name=".pages.HowToActivity" />
        <activity android:name=".pages.AboutActivity" />
        <activity android:name=".pages.ResultDetailActivity" />
        <activity android:name=".pages.ResultActivity" />
        <activity android:name=".pages.KPSPActivity" />
        <activity android:name=".pages.FormActivity" />
        <activity
            android:name=".pages.HomeActivity"
            android:theme="@style/AppTheme.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.TestActivity" />
    </application>

</manifest>